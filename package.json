{
  "name": "@napp/exception",
  "version": "9.1.4",
  "description": "common exception. node application common library. serialization error",
  "types": "dist/index.d.ts",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clear": "npx rimraf --glob dist/**",
    "upversion": "npm version patch && node make_version.mjs",
    "tsc": "tsc -p tsconfig.json",
    "build": "npx tsup src/index.ts --format cjs,esm --dts --sourcemap --clean",
    "prerelease": "npm run upversion",
    "release": "npm run build -- --minify",
    "prepush": "npm run test && npm run release",
    "push": "npm publish --access=public",
    "test": "node --test --import=tsx test/**/*.test.ts"
  },
  "author": "farcek@gmail.com",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/napp-core/exception.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "error",
    "errors",
    "exception",
    "common error",
    "error serialization"
  ],
  "devDependencies": {
    "@types/node": "^24.10.0",
    "rimraf": "^6.1.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
